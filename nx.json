{
  "$schema": "./node_modules/nx/schemas/nx-schema.json",
  "neverConnectToCloud": true,
  "analytics": false,
  "namedInputs": {
    "source": ["{projectRoot}/src/**", "{projectRoot}/tsconfig.json"],
    "packageFiles": ["{projectRoot}/package.json", "{workspaceRoot}/package-lock.json"],
    "lintConfig": ["{projectRoot}/oxlint.config.ts"],
    "fmtConfig": ["{projectRoot}/oxfmt.config.ts"],
    "testConfig": ["{projectRoot}/vitest.config.ts"]
  },
  "targetDefaults": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["source", "packageFiles"],
      "outputs": ["{projectRoot}/dist/**"],
      "cache": true
    },
    "test": {
      "dependsOn": ["^build"],
      "inputs": ["source", "packageFiles", "testConfig", "{projectRoot}/__tests__/**"],
      "cache": true
    },
    "lint": {
      "inputs": ["source", "lintConfig"],
      "cache": true
    },
    "lint:fix": {
      "inputs": ["source", "lintConfig"],
      "cache": false
    },
    "fmt:check": {
      "inputs": [
        "{projectRoot}/src/**",
        "{projectRoot}/__tests__/**",
        "{projectRoot}/*.json",
        "{projectRoot}/*.md",
        "fmtConfig"
      ],
      "cache": true
    },
    "fmt": {
      "cache": false
    }
  }
}
